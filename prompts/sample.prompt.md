---
mode: 'ask'
description: 'Sample prompt — replace with your own task description'
---

<!--
  Replace this comment and the lines below with your actual prompt.
  Use #{selection} to reference selected code, or #{file:path/to/file} to include a file.
-->

Describe the task you want Copilot to perform. Be specific about:
- **Input**: what the code or context looks like before the change.
- **Output**: what you expect as the result.
- **Constraints**: language, framework, style rules, etc.

Example (delete this block):
> Refactor the selected function to use async/await instead of callbacks.
> Preserve the existing public API and add a JSDoc comment.
